name: Hello World Workflow

on:
  workflow_dispatch:
  push:
    branches:
      - main
      
  
jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Hello, GitHub Actions! 🚀"
